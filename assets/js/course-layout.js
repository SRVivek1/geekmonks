function slugify(str){return str.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"")}document.addEventListener("DOMContentLoaded",function(){const links=document.querySelectorAll(".topic-link");const contentDiv=document.getElementById("topic-content");const courseLinks=document.querySelectorAll(".course-link");const{baseurl:baseurl,defaultTopic:defaultTopic}=window.courseConfig;function loadTopic(coursePath,topicId){fetch(`${baseurl}${coursePath}/${topicId}`).then(response=>response.text()).then(md=>{const topicName=topicId.replace(/-/g," ").toUpperCase();const tContent=md;contentDiv.innerHTML=tContent})["catch"](err=>console.error("Error loading topic:",err))}links.forEach(link=>{link.addEventListener("click",function(e){e.preventDefault();links.forEach(l=>l.classList.remove("active"));courseLinks.forEach(cl=>{const topicList=document.getElementById(`topics-${slugify(cl.dataset.course)}`);if(topicList)topicList.classList.remove("collapse")});this.classList.add("active");const topicId=this.dataset.topic;const coursePath=this.dataset.coursePath;loadTopic(coursePath,topicId);const topicList=document.getElementById(`topics-${slugify(coursePath.split("/").pop())}`);if(topicList)topicList.classList.remove("collapse");const sidebar=document.getElementById("course-sidebar");if(sidebar&&sidebar.classList.contains("show")){sidebar.classList.remove("show")}})});const sidebarToggle=document.getElementById("sidebarToggle");const sidebar=document.getElementById("course-sidebar");if(sidebarToggle&&sidebar){sidebarToggle.addEventListener("click",function(){sidebar.classList.toggle("show")})}const defaultLink=document.querySelector(`[data-topic="${defaultTopic}"]`);if(defaultLink)defaultLink.click()});